<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:artifact="antlib:org.apache.maven.artifact.ant"
         name="build"
         default="buildJsDoc">
	<property file="build.properties" />

	<property name="version" value="${coreVersion}" />
	<property name="qualifier" value="${coreQualifier}" />

	<taskdef resource="net/sf/antcontrib/antlib.xml"
	         classpath="lib/ant-contrib-1.0b3.jar" />
	<taskdef resource="com/bstek/dorado/addonbuilder/task.properties"
	         classpath="lib/dorado-addon-builder-1.0.0.jar" />

	<target name="buildJsDoc">
		<delete dir="${rootWorkDir}/docs" />
		<mkdir dir="${rootWorkDir}/docs" />

		<buildDoradoAddon baseDir="${workspaceDir}/core"
		                  workDir="${rootWorkDir}/docs"
		                  targetDir="${targetDir}"
		                  name="dorado-docs"
		                  vendor="${vendor}"
		                  version="${version}"
		                  qualifier="${qualifier}"
		                  compileJava="false"
		                  buildJar="false"
		                  buildSourceJar="false"
		                  buildJsDoc="true"
		                  buildJavaDoc="true"
		                  makePom="false"
		                  clientSrc="${workspaceDir}/client/client,${workspaceDir}/chart/client,${workspaceDir}/desktop/client,${workspaceDir}/htmleditor/client"
		                  javaSrc="${workspaceDir}/core/src,${workspaceDir}/data/src,${workspaceDir}/web/src,${workspaceDir}/view/src,${workspaceDir}/hibernate/src" />
	</target>
</project>
